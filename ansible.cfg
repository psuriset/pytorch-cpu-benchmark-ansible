[defaults]
inventory = inventory/hosts.ini
# Playbook lives under playbooks/; roles/ is at repo root — add it to the search path
roles_path = roles
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
forks = 20
timeout = 60

[ssh_connection]
pipelining = True
