# pytorch-cpu-benchmark-ansible
Ansible + PyTorch + CPU benchmark
This repo installs PyTorch on target hosts, runs CPU benchmarks, saves logs on the remote host, and fetches results back to the control machine.

